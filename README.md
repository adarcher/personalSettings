*This is not for public use but if you come across it and think some random CSS is useful, there is no license and you may do what you want with it.*

VSCode stopped allowing local relative paths in settings.  I wanted to have a global style for my Markdown previews and using a weblink is one of the options.  The issue with that though is that VSCode caches the file, and if you change it, it won't know when to redownload it.
